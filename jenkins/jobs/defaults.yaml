- defaults:
    name: global
    project-type: freestyle
    concurrent: false
    quiet-period: 5
    retry-count: 5
    properties:
      - build-discarder:
          days-to-keep: 3
          num-to-keep: 20
      - rebuild
    wrappers:
      - timestamps
